#include <stdio.h>

/**
 * main - Prints singles digit numbers of base ten
 * Return: Always 0 (success)
 */
int main(void)
{
	int i = 0;

	for (i = 0; i <= 10; i++)
	printf("%d", i);
	printf('\n');
	return (0);
}
