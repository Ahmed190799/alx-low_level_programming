#include "main.h"

/**
 * _memset - Entry point
 * @n: bytes
 * @s: pointer destination
 * @b: constant byte
 * Return - s
 */
char *_memset(char *s, char b, unsigned int n);
{
	unsigned int i;

	if (i = 0, i < n, i++)
		s[i] = b;
	return (s);
}

